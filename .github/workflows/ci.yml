name: Portfolio CI

on:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Verify HTML files
        run: |
          echo "Checking project files..."
          ls -la

      - name: Build Successful
        run: echo "Portfolio build completed successfully!"